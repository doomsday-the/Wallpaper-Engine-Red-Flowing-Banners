// Default Configuration Settings
// These will be overridden by Wallpaper Engine's property system when loaded in WE.

window.WallpaperSettings = {
    breathingEnabled: true,
    breathingSpeed: 0.0125, // Extremely slow
    breathingAmplitude: 0.05, 

    lightingEnabled: true,
    lightingIntensity: 2.5,
    lightingSpeed: 0.0125, // Extremely slow

    deformationEnabled: true,
    deformationAmount: 0.03, // Internal deformation amount



    parallaxEnabled: true,
    parallaxAmount: 3.0,
    parallaxSmoothing: 0.05, // Lower is smoother/slower


    // Internal state
    mouseX: 0.5,
    mouseY: 0.5,
    targetMouseX: 0.5,
    targetMouseY: 0.5
};
